{
  "name": "catch-catch-server",
  "version": "1.0.0",
  "description": "WebSocket relay server for catch-catch multiplayer cat overlay",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
